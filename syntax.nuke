/*
 *  test.nuke - Null
 */

# Including a file will just import the entire file.
import `test.nuke`

setAsArray(@in) {
  in = [in]
}

apples

a = 256
a = "Hello, World!";
b = 0.50343245+a
b = true
setAsArray(a)
a.append(b)

main() {
  while("Hello, World!")
  test()
  // This function automatically will run on start
}
